__pycache__/
**/__pycache__/**
*.pyc
.DS_Store
.git
.gitignore

# Project dirs not needed at runtime
# Note: frontend/build/ IS needed - it's served by the API
frontend/node_modules/
frontend/src/
frontend/public/
mobile/
training/
PlantVillage/

# Python virtual environments
api/venv/

# Dependencies (frontend/mobile already excluded, but root-level node_modules/ would sneak in)
node_modules/

# Config, docs, checkpoints
*.md
*.config.example
.env*
# Root-level h5 checkpoints (NOT saved_models — those are needed at runtime)
/*.h5
